CREATE DATABASE CRUD_WINFORMS

USE CRUD_WINFORMS

CREATE TABLE PRODUTO
(
	ID INT IDENTITY(1,1) PRIMARY KEY,
	NOME VARCHAR(MAX),
	PRICE FLOAT
)

CREATE TABLE ESTOQUE
(
	ID INT IDENTITY(1,1) PRIMARY KEY,
	FK_PRODUTO INT REFERENCES PRODUTO (ID),
	QUANTIDADE INT
)